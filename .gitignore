# Python
__pycache__/
*.pyc
.pytest_cache/

# Vibe Studio Code (upstream checkout, toolchain and build output are never tracked)
vibe/vscode/
vibe/.toolchain/
vibe/.build/
vibe/VSCode-*/
# Overlay files are tracked verbatim: no rule above (*.log, progress/, *-database/, ...) may hide one.
!vibe/overlay/**
