# Python
__pycache__/
*.pyc
.pytest_cache/

# Upstream checkout, toolchain and build output are never tracked. Anchored, so that a
# folder of the same name inside overlay/ stays tracked.
/vscode/
/.toolchain/
/.build/
/VSCode-*/
/vscode-reh-*/
/vscode-server-*/
