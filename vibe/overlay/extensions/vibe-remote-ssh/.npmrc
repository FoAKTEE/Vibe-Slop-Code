# ssh2 builds optional native addons from its install script: never run it,
# the extension uses the pure JavaScript fallback and must not need a compiler.
ignore-scripts=true
omit=optional
legacy-peer-deps="true"
